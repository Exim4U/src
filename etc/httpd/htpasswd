# Put user and encrypted password here for webaccess to MailHub4u menu system.
#
# Generate Password with:
# htpasswd -c /etc/httpd/htpasswd root
# Then, specify the password in reply to the dialog.
#
# Entry looks like the following line:
#root:<encrypted password>

